# -*- Mode: org -*-
# -*- coding: utf-8 -*-

#+title: Faust Autodiff: Towards Domain Specific Machine Learning
#+author: Thomas Rushton

#+startup: overview
#+export_file_name: out/aes-aimla-2025
#+options: toc:nil num:5 H:5 date:nil author:nil title:nil
#+latex_class: aes-aimla-2025
#+bibliography: refs.bib
# Might have to revert to natbib...
#+cite_export: biblatex
#+latex_header_extra: \RequirePackage[]{./out/aes-aimla-2025}

#+begin_export latex
\twocolumn[
\maketitle % MANDATORY!

\begin{onecolabstract}
Write the abstract here.
\end{onecolabstract}
]
#+end_export

* About                                                            :noexport:

* LaTeX Setup                                                      :noexport:
:PROPERTIES:
:header-args: :tangle out/aes-aimla-2025.sty :results silent
:END:

** Provide the package

#+begin_src latex
\ProvidesPackage{aes-aimla-2025}[2025/01/15 v0.1 Bundling AES 2025 AIMLA style]
#+end_src

** Packages

UTF-8 encoding is recommended but use one that works for you.

#+begin_src latex
\usepackage[utf8]{inputenc}
#+end_src

Highly recommended package for better looking text automatically.

#+begin_src latex
\usepackage{microtype}
#+end_src

Natbib is used for more control on citations. You can use other modern
bibliography packages but please try to match the provided style.

#+begin_src latex
%% \usepackage[numbers,square]{natbib}
#+end_src

"These are useful for different purposes."

#+begin_src latex
\usepackage{booktabs}
\usepackage{color}
\usepackage{url}
\usepackage{tikz}
\usetikzlibrary{positioning,calc,intersections}
#+end_src

Maths stuff.

Why it's necessary to use a negative top margin is... mysterious.

#+begin_src latex
\usepackage{mdframed}
\mdfsetup{skipabove=0pt,
  skipbelow=0pt,
  linewidth=.5pt,
  innertopmargin=-10pt
  innerleftmargin=0pt
}
\usepackage{amsmath}
\usepackage{amssymb}
\usepackage{stmaryrd}
#+end_src

More useful stuff

#+begin_src latex
\usepackage{hyperref}
\hypersetup{hidelinks}
\usepackage[noabbrev,capitalise]{cleveref}
%% \crefname{lstlisting}{listing}{listings}
\Crefname{lstlisting}{Listing}{Listings}
#+end_src

Listings. I should consider using [[https://www.reddit.com/r/emacs/comments/lbkmmz/the_best_syntax_highlighting_in_a_pdf_youll_see_a/][engrave-faces]] instead.

#+begin_src latex
\usepackage{color}
\usepackage{listings}
\definecolor{mygrey}{rgb}{0.96,0.96,0.96}
\lstdefinelanguage{faust}{
  morekeywords={process,with,environment},
  morecomment=[l]{//},
}
\lstset{
  tabsize=4,
  basicstyle=\ttfamily,
  commentstyle=\color{black!55!white},
  identifierstyle=\color{black!75!white},
  lineskip=-.1em,
  basewidth=0.55em,
  backgroundcolor=\color{mygrey},
  captionpos=b,
  breaklines=true,
  framexleftmargin=2pt,
  framexrightmargin=2pt,
  framextopmargin=2pt,
  framexbottommargin=2pt,
  frame=tb,
  framerule=0pt
}
\usepackage[varqu,varl]{inconsolata}
#+end_src

** Figures

Graphics path.

#+begin_src latex
\graphicspath{{./}{figures/}}
#+end_src

Subcaptions & SVGs.

#+begin_src latex
\usepackage{subcaption}
\usepackage[inkscapelatex=false]{svg}
#+end_src

** Authors

Put the authors in order here. The number in brackets define the
corresponding affiliation.

#+begin_src latex
\author[1]{Thomas Rushton}
\author[1]{Yann Orlarey}
\author[1]{Romain Michon}
\author[1]{Tanguy Risset}
#+end_src

Affiliations go here.

#+begin_src latex
\affil[1]{Inria, INSA Lyon, EA3720 \\ 69621 Villeurbanne, France}
#+end_src

Correspondece should include the corresponding author's name and
e-mail address

#+begin_src latex
\correspondence{Thomas Rushton}{thomas.rushton@inria.fr}
#+end_src

If there are many authors, please use the form "First author et al."
#+begin_src latex
\lastnames{Rushton, Orlarey, Michon, and Risset}
#+end_src

Short title should describe your topic but not be too long.
#+begin_src latex
\shorttitle{Faust Autodiff}
#+end_src

** Bibliography

#+begin_src latex
\bibliographystyle{jaes}
#+end_src

* Introduction

Differentiable programming via automatic differentiation (AD) is the
bedrock upon which gradient-based mathematical optimisation techniques
lie, and forms the basis for current approaches to machine
learning. Differentiability of the primitive operators of a
programming language permits the creation of programs that are
end-to-end differentiable with respect to their input variables;
coupled with a loss function --- also composed of differentiable
primitives --- such a program can produce gradients, which, when
backpropagated as update-values to the input variables, cause those
inputs to approach optimal values. Differentiable digital signal
processing (DDSP) is concerned with the creation and application of
differentiable signal processors to mathematical optimisation tasks,
from the LMS algorithm to machine learning.

Having previously demontstrated that DDSP is possible in the Faust
language, here we extend to a more general AD syntax and an
accompanying set of programmatic conventions. We show that IIR filters
can be made differentiable in Faust and their coefficients optimised
via gradient descent. We also demonstrate that neural network
structures can be created in a composable, extensible fashion in
Faust, pointing the way toward an audio-focused ecosystem for the
development of solutions to problems based on machine learning
approaches. Though imposing a particular syntactic approach,
dispensing with support for certain features of the Faust language, we
contend that such restrictions are comparable with those posed by
established automatic differentiation and machine learning
frameworks.

* The Faust Language

#+begin_src faust :results file svg :file images/seq.svg :exports none
A = _ <: par(n, 20, -) :> _;
B = _ <: par(n, 20, +) :> _;
process = A : B;
#+end_src

#+RESULTS:
[[file:images/seq.svg]]

#+begin_src faust :results file svg :file images/par.svg :exports none
A = _ <: par(i, 20, +) :> _;
B = _ <: par(i, 20, -) :> _;
process = A,B;
#+end_src

#+RESULTS:
[[file:images/par.svg]]

#+begin_src faust :results file svg :file images/rec.svg :exports none
A = _,_ <: par(i, 20, +) :> _;
B = _ <: par(i, 20, -) :> _;
process = A ~ B;
#+end_src

#+RESULTS:
[[file:images/rec.svg]]

#+begin_src faust :results file svg :file images/split.svg :exports none
A = _ <: par(i, 20, +) :> _,_;
B = _,_,_,_ <: par(i, 20, -) :> _;
process = A <: B;
#+end_src

#+RESULTS:
[[file:images/split.svg]]

#+begin_src faust :results file svg :file images/merge.svg :exports none
A = _ <: par(i, 20, +) :> _,_,_,_;
B = _,_ <: par(i, 20, -) :> _;
process = A :> B;
#+end_src

#+RESULTS:
[[file:images/merge.svg]]

#+name: fig:compositions
#+caption: Example compositions of Faust blocks.
\begin{figure*}[t]
  \begin{minipage}[b]{\dimexpr(\linewidth-\leftmargin)*18/100\relax}
    \centering
    \includesvg[width=\textwidth]{images/par}
    \subcaption{Parallel, \(A,B\).}
    \label{fig:par}
  \end{minipage}
  \begin{minipage}[b]{\dimexpr(\linewidth-\leftmargin)*20/100\relax}
    \centering
    \includesvg[width=\textwidth]{images/seq}
    \subcaption{Sequential, \(A:B\).}
    \label{fig:seq}
  \end{minipage}
  \begin{minipage}[b]{\dimexpr(\linewidth-\leftmargin)*22/100\relax}
    \centering
    \includesvg[width=\textwidth]{images/split}
    \subcaption{Split, \(A<:B\).}
    \label{fig:split}
  \end{minipage}
  \begin{minipage}[b]{\dimexpr(\linewidth-\leftmargin)*20/100\relax}
    \centering
    \includesvg[width=\textwidth]{images/rec}
    \subcaption{Recursive, \(A\sim{}B\).}
    \label{fig:rec}
  \end{minipage}
  \begin{minipage}[b]{\dimexpr(\linewidth-\leftmargin)*22/100\relax}
    \centering
    \includesvg[width=\textwidth]{images/merge}
    \subcaption{Merge, \(A:>B\).}
    \label{fig:merge}
  \end{minipage}
\end{figure*}

Faust is a functional paradigm, domain specific language (DSL) for
audio digital signal processing [cite:@orlareyFAUST2009]. It is
built atop a domain-agnostic /block diagram algebra/ (BDA)
[cite:@orlareyAlgebra2002] that describes primitive operations (though
the nature of these operations is not important at the level of the
BDA), connected into graphs via a selection of /composition operators/
(see cref:fig:compositions). A textual language, Faust can, for
example, define a diagram consisting of two blocks, \(A\) and \(B\),
composed in sequence, with code of the form
#+name: lst:seq
#+caption: Sequential compostion of two Faust blocks.
#+begin_src faust :noexec
process = A : B;
#+end_src
Conceptually, a given block diagram, \(D\), may undergo semantic
transformation, notated \(\llbracket \cdot \rrbracket\), into a signal
processor [cite:@orlareySyntactical2004]:
#+NAME: eq:semanticTransformation
\begin{equation}
  \begin{split}
    \llbracket D \rrbracket &\;:\; \mathbb{S}^{n} \to \mathbb{S}^{m} \\
    \llbracket D \rrbracket(s_{1},\dots,s_{n}) &\to (y_{1},\dots,y_{m}) \\
    \mathbf{y}(t) &= f(\mathbf{s}(t))\;,
  \end{split}
\end{equation}
where \(\mathbb{S}\) is the set of all signals, \(f\) is a
mathematical function analogous to the primitive operations and
compositions of \(D\), and \(t\) is time. The signal processor
receives a vector of input signals, \(\mathbf{s}\), and produces a
vector of output signals \(\mathbf{y}\).

** The Faust Ecosystem

Faust is a highly optimised, compiled language [cite:@orlareyNew2020],
with C++ as its default compilation target. One of the great strengths
of the Faust ecosystem is its /write once, run anywhere/ model; a
variety of additional backends are supported, including LLVM
[cite:@letzOverview2018], WebAssembly [cite:@letzCompiling2017], and
Rust [cite:@gasterAudio2020], plus numerous software and hardware
platforms. Faust code can be compiled for use in VST
plugins[cite:@albouyFaust2017], for embedded systems
[cite:@michonEmbedded2020] and FPGAs [cite:@cochardProgramming2024];
it is used by artists and digital luthiers
[cite:@michonFaust2020a;@rushtonSpringamajig2022;@hansenWeather2018],
and features sophisticated libraries for wave digital filter modelling
[cite:@roosenburgWave2021] and physical modelling synthesis
[cite:@russoIntroducing2021;@michonFaust2018], amongst others.

** Differentiable Programming in Faust

In light of Faust's versatility, efficiency, and strengths as a DSL in
tackling audio problems it is little suprise that it has been turned
to machine learning problems in the audio domain, via /DawDreamer/
[cite:@braunDawDreamer2021], which uses a Python backend to transpile
Faust code for gradient-based optimisation with the JAX Python
library. JAX, and other well-known machine learning frameworks, such
as TensorFlow and PyTorch, enable gradient-based optimisation
approaches by way of /automatic differentiation/, a technique for
computing the numerical outputs of both a mathematical expression (or,
more commonly, a computer program) and its derivatives in parallel
[cite:@rallArithmetic1986;@baydinAutomatic2018;@harrisonBrief2021].

/Differentiable programming/ [cite:@wangDemystifying2019] is, broadly
speaking, the creation of computer programs using primitive operations
which are themselves differentiable; a computer program consisting of
differentiable primitives may be differentiated automatically,
producing the desired /primal/ and /gradient/ numerical outputs for
use in a gradient-based optimisation algorithm. The feasibility of
differentiable programming, and thus DDSP, in the Faust language has
been demonstrated via the implementation of forward-mode AD primitives
[cite:@rushtonDifferentiable2024].

One way of framing forward mode AD is as an arithemitic of dual
numbers [cite:@rallArithmetic1986; @yuDNAD2013] of the form
\(\langle{}u,\nabla{}u\rangle\), where \(u\) is the aforementioned primal, and \(\nabla{}u
= \begin{bmatrix}\frac{\partial{}u}{\partial{}x_{1}} & \cdots & \frac{\partial{}u}{\partial{}x_{N}}\end{bmatrix}^{T}\)
the gradient of \(u\), its partial derivatives with to a vector of
\(N\) input variables, \(\mathbf{x}\). A dual number can be passed
into a forward-mode differentiable primtive, \(f\), yielding an output
dual number, \(\langle{}f(u),\nabla{}uf'(u)\rangle\). Faust is a language of /signals/ and
signal processors; consequently we will refer to the /dual signal/
\(\langle{}s,\nabla{}s\rangle \in \mathbb{S}_{d}\), where \(\mathbb{S}_{d} = \mathbb{S}^{N+1}\). The
signal processor based on the differentiated block diagram \(D\) has
the following semantic scheme:
#+NAME: eq:diffSemantic
\begin{equation}
\begin{split}
\Delta_{N}\llbracket D \rrbracket &\;:\; \mathbb{S}_{d}^{M} \to \mathbb{S}_{d}^{P} \\
\Delta_{N}\llbracket D \rrbracket(\langle{}s_{1},\nabla{}s_{1}\rangle,&\dots,\langle{}s_{M},\nabla{}s_{M}\rangle) \\
&\to (\langle{}y_{1},\nabla{}y_{1}\rangle,\dots,\langle{}y_{P},\nabla{}y_{P}\rangle) \\
\langle\mathbf{y}(t), \;\mathbf{J}_{\mathbf{y}}(t)\rangle &= \langle{}f(\mathbf{s}(t)), \;\mathbf{J}_{{\mathbf{s}}}(t)\mathbf{J}_{f}(t))\rangle\;,
\end{split}
\end{equation}
where \(\Delta_{N}\llbracket \cdot \rrbracket\) indicates derivative
transformation with respect to \(N\) input variables, and
\(\mathbf{J}_{\mathbf{y}}\), \(\mathbf{J}_{\mathbf{s}}\), and
\(\mathbf{J}_{f}\) are, respectively, \(N\times{}P\), \(N\times{}M\) and \(M\times{}P\)
Jacobian matrices.

AD primitives have been demonstrated in Faust using the language's
pattern-matching abstraction
[cite:@rushtonDifferentiable2024]. Pattern-matching can be thought of
as something akin to the operator overloading approach that is
possible in languages such as Python and C++, and exploits the fact
that a Faust function definition may receive, as a parameter, a
programmatic expression. For example, a differentiable addition
primitive, acting on dual signals \(\langle{}s_{1}, \nabla{}s_{1}\rangle,\langle{}s_{2}, \nabla{}s_{2}\rangle\), may be
created by defining a function that accepts the addition operator as a
parameter, and produces, in parallel, the addition primitive's primal
output, \(s_{1}+s_{2}\), and its gradients, \(\nabla{}s_{1}+\nabla{}s_{2}\) (see cref:lst:diffAdd).
#+name: lst:diffAdd
#+caption: Definition of a differentiable addition primitive in the
#+caption: Faust language.
#+attr_latex: :float t 
#+begin_src faust :noeval
diff(+) = interleave(N + 1, 2)
    : +,par(i, N, +);
#+end_src
The function ~interleave~ ensures that the primal signals and their
respective partial derivatives are delivered to the addition
operations in the appropriate order: \(s_{1}, s_{2}, \frac{\partial{}s_{1}}{\partial{}x_{1}},
\frac{\partial{}s_{2}}{\partial{}x_{1}},\dots,\frac{\partial{}s_{1}}{\partial{}x_{N}}, \frac{\partial{}s_{2}}{\partial{}x_{N}}\), as illustrated
in cref:fig:diffadd. One addition primitive is created for the primal
operation, and a further \(N\) additions in parallel for computing
partial derivatives, which we can notate \(\big\|_{l=1}^{N} +\).

#+begin_src faust :results file svg :file images/diffadd.svg :exports results
N = 2;
distribute(0, q, 0) = par(i, q, _);
distribute(p, q, 0) = par(i, p, !),par(j, q, _);
distribute(0, q, r) = par(i, q, _),par(j, r, !);
distribute(p, q, r) = par(i, p, !),par(j, q, _),par(k, r, !);
interleave(R, C) = par(i, R * C, _) <: par(r, R, par(c, C, distribute(r, 1, R - r - 1)));
diff(+) = interleave(N + 1, 2) : +,par(i, N, +);
diff(_) = par(i, N+1, _);
// A = 1 <: par(i, 12, +) :> _,_,_;
// B = 1 <: par(i, 12, -) :> _,_,_;
process = diff(_),diff(_) : diff(+);
#+end_src
#+attr_latex: :width .75\linewidth :float t
#+name: fig:diffadd
#+caption: Differentiable addition primitive applied to dual signals
#+caption: \(\langle{}s_{1}, \nabla{}s_{1}\rangle,\langle{}s_{2}, \nabla{}s_{2}\rangle\); implicit input variable
#+caption: vector \(\mathbf{x}\) of length \(N = 2\).
#+RESULTS:
[[file:images/diffadd.svg]]

** Motivation

Considering the plethora of existing approaches to DDSP
[cite:@hayesReview2023], why pursue an implementation in Faust? The
availability of the Faust-to-JAX transpiler would appear to obviate
such an endeavour, until one considers that the AD transformations
facilitated by JAX lie the wrong side, as it were, of that
transpilation. A comprehensive differentiable programming framework in
the Faust language itself would allow DDSP algorithms to be written
once, and run anywhere.

Furthemore, The observation has been made that the relative structures
of neural networks and digital filters bear striking similarities
[cite:@kuznetsovDifferentiable2020]. There is a very real sense in
which machine learning problems, and gradient-based optimisation
problems in general, are signal processing problems. The possibility
of turning a signal processing DSL to problems of this sort paves the
way to novel approaches and fresh perspectives on this vibrant field
of research.

* A General Scheme for Differentiating Faust Programs

#+name: fig:diffRules
#+caption: Automatic differentiation rules for Faust's composition operators,
#+caption: plus supporting primitive operators and routing expressions. Use of
#+caption: \(\Delta_{N}\llbracket \texttt{mem} \rrbracket\) is implicit in the
#+caption: definition of \(\Delta_{N}\llbracket A \sim B\rrbracket\). \(N\) is the
#+caption: number of input variables with respect to which partial derivatives are
#+caption: calculated.
\begin{figure*}
  \begin{mdframed}
    \begin{minipage}[c]{\dimexpr\linewidth*18/40\relax}
      \begin{gather}
        \Delta_{N}\llbracket A,B \rrbracket =
          \Delta_{N}\llbracket A \rrbracket,\Delta_{N}\llbracket B \rrbracket \\
        \Delta_{N}\llbracket A : B \rrbracket =
          \Delta_{N}\llbracket A \rrbracket : \Delta_{N}\llbracket B \rrbracket \\
        \Delta_{N}\llbracket A <: B \rrbracket =
          \Delta_{N}\llbracket A \rrbracket <: \Delta_{N}\llbracket B \rrbracket \\
        \Delta_{N}\llbracket A \sim B \rrbracket =
          \Delta_{N}\llbracket A \rrbracket \sim \Delta_{N}\llbracket B \rrbracket \\
        \Delta_{N}\llbracket A :> B \rrbracket =
          \Delta_{N}\left\llbracket A : \chi(i_{B},k)\right\rrbracket : F : \Delta_{N}\llbracket B \rrbracket
          \label{eq:diffmerge} \\
        F = \big\|_{l=1}^{i_{B}}\left(
          \big\|_{l=1}^{k}\Delta_{N}\llbracket\_\rrbracket :> \Delta_{N}\llbracket\_\rrbracket
        \right) \nonumber \\
        k = \frac{o_{A}}{i_{B}} \nonumber
      \end{gather}
    \end{minipage}
    \begin{minipage}[c]{\dimexpr\linewidth*22/40\relax}
      \begin{gather}
        \textrm{Parallel iteration, }\texttt{par(l, L, D)}\textrm{:} \nonumber \\
        \big\|_{l=1}^{L}D = D_{1},\dots,D_{L} \\
        \textrm{Signal interleaving function: }\nonumber \\
        \chi(r,c) = \big\|_{l=1}^{r\times{}c}\_ <: \big\|_{m=0}^{r-1}
          \big\|_{n=1}^{c}\left(
            \big\|_{p=1}^{m}!,\_,\big\|_{q=1}^{r-m-1}!
          \right) \label{eq:interleave} \\
        \Delta_{N}\llbracket \_ \rrbracket = \big\|_{l=1}^{N+1}\_ \\
          \Delta_{N}\llbracket ! \rrbracket = \big\|_{l=1}^{N+1}! \\
        \Delta_{N}\llbracket\texttt{mem}\rrbracket = \big\|_{l=1}^{N+1}\texttt{mem}
      \end{gather}
    \end{minipage}
  \end{mdframed}
\end{figure*}

Faust is based on a /block-diagram algebra/
[cite:@orlareySyntactical2004]. In this algebra, a given block
diagram, \(D\), is comprised of a primitive block, \(P\), or the
composition of two block diagrams via a composition operator according
to the syntactic rule[fn:1]
#+NAME: eq:syntax
\begin{equation}
D = P \;|\; (D_{1} \diamond D_{2})\;,
\end{equation}
where \(\diamond\) is one of the sequential (:), parallel (,), split (<:),
merge (:>), or recursive (\(\sim\)) composition operators. The creation
of differentiable Faust programs has been shown to be possible by
/undifferentiated/ composition of differentiated primitive blocks
[cite:@rushtonDifferentiable2024], the syntactic rule being
#+NAME: eq:badDiffSyntax
\begin{equation}
\Delta_{N}\llbracket D \rrbracket = \Delta_{N}\llbracket P_{1} \rrbracket \diamond \Delta_{N}\llbracket P_{2} \rrbracket \diamond \dots \diamond \Delta_{N}\llbracket P_{n} \rrbracket\;.
\end{equation}
While effective in facilitating differentiable programming, this
syntax imposed an undesirable verbosity. Our preferred syntactic rule
should be a direct transformation of cref:eq:syntax:
#+NAME: eq:idealDiffSyntax
\begin{equation}
\Delta_{N}\llbracket D \rrbracket
  = \Delta_{N}\llbracket P \rrbracket \;|\; \Delta_{N}\llbracket D_{1} \diamond D_{2} \rrbracket \;.
\end{equation}
The ideal outcome is to permit the programmer to subject an entire
Faust program to automatic differentiation in a single expression,
minimising the amount of re-writing required to ready their code for
transformation. This would render a great proportion of existing Faust
code differentiable, and, in doing so, prepare that wealth of material
for prospective use in optimisation problems, and machine learning
applications. In order to achieve such an outcome, it is necessary to
provide differentiation rules for the five composition operators,
taking into account their arithmetical significance, if any, in a
block diagram to which they contribute.

** Differentiable Block-Diagram Compositions

To define syntactic rules for the derivatives of Faust's composition
operators entails analysing them as if they were themselves primitive
blocks. To do this, we must consider them in tandem with rules for
some of Faust's fundamental primitive operators; these will assist us
in achieving differentiable compositions.

*** Identity Primitive

Faust's identity function, notated \(\_\), describes a connection
between two blocks. Treated as a signal processor, it, adheres to the
following semantic scheme [cite:@orlareySyntactical2004]:
#+NAME: eq:identity
\begin{equation}
  \begin{split}
    \llbracket \_ \rrbracket &\;:\; \mathbb{S} \to \mathbb{S} \\
    \llbracket \_ \rrbracket(s) &\to (s)\;.
  \end{split}
\end{equation}
It receives one signal and returns that same signal unchanged,
analogous to multiplying it by a constant signal of value
\(1\). In dual signal arithmetic, this is
#+NAME: eq:dualIdentity
\begin{align}
\langle{}s,\nabla{}s\rangle\langle{}1,\nabla1\rangle
  = \langle{}1s, 1\nabla{}s + s\nabla{}1 \rangle
  = \langle{}s,\nabla{}s\rangle\;,
\end{align}
and the semantic rule for the derivative of the identity function is
thus
#+NAME: eq:diffidentity
\begin{equation}
  \begin{split}
    \Delta_{N}\llbracket \_ \rrbracket &\;:\; \mathbb{S}_{d} \to \mathbb{S}_{d} \\
    \Delta_{N}\llbracket \_ \rrbracket(\langle{}s, \nabla{}s\rangle) &\to (\langle{}s, \nabla{}s\rangle)\;.
  \end{split}
\end{equation}
The dual signal \(\langle{}s,\nabla{}s\rangle\) being composed of one
primal signal and \(N\) gradient signals, we can further state that
#+NAME: eq:diffidentity2
\begin{equation}
  \Delta_{N}\llbracket \_ \rrbracket = \big\|_{l=1}^{N+1}\;\_\;.
\end{equation}

*** Parallel Composition

Blocks composed in parallel (see cref:fig:par) do not influence
one-another, arithemtically-speaking. The outputs of the program
\(A,B\), \(y_{1},\dots,y_{o_{A}}\) and \(y_{o_{A}+1},\dots,y_{o_{A}+o_{B}}\), are
essentially the outputs of separate block diagrams; thus
\(\Delta_{N}\llbracket A,B \rrbracket = \Delta_{N}\llbracket A \rrbracket,\Delta_{N}\llbracket
B \rrbracket\), and, more generally
#+NAME: eq:diffpar
\begin{equation}
\Delta_{N}\left\llbracket\big\|_{l=1}^{L}D\right\rrbracket =
  \big\|_{l=1}^{L}\Delta_{N}\llbracket D \rrbracket \;.
\end{equation}

*** Sequential Composition

#+begin_export latex
\pgfkeys{
  /diffop/.cd,
  faust/.initial=,
  math/.initial=,
}
\newcommand\diffop[1]{
  \pgfkeys{/diffop/.cd,#1}
  \begin{scope}[
    faustnode/.style={rectangle, thick, draw=black, minimum size=7mm}
  ]
    \draw[] (box) (0,0) rectangle (3.5,2.8)
    
    \node[faustnode](mul) at (2.7,.7) {\(\times\)}
    \node[faustnode](dop)[above left=-2mm and 6mm of mul]{\(\nabla\pgfkeysvalueof{/diffop/math}\)}
    \node[faustnode](op)[above=2mm of dop]{\(\pgfkeysvalueof{/diffop/math}\)}
    \node(label)[above right=28mm and 11.5mm of box]{\(\Delta_{N}\llbracket \pgfkeysvalueof{/diffop/faust} \rrbracket\)}

    \draw[thick, ->] let \p{opw}=(op.west) in (0,\y{opw})--(op.west)
    \draw[thick, ->] let \p{opw}=(op.west), \p{dopw}=(dop.west) in
      (0,\y{opw})--+(.5,0)--(.5,\y{dopw})|-(dop.west)
    \draw[thick, ->] let \p{ope}=(op.east) in (op.east)--(3.5,\y{ope})
    \draw[thick, ->] let \p{mulw}=(mul.west) in (dop.east)--+(.3,0)|-(\x{mulw},.9)
    \draw[thick, ->] let \p{mulw}=(mul.west) in (0,\y{mulw})--+(.5,0)--+(.5,-.15)|-+(\x{mulw},-.15)
    \draw[thick, ->] let \p{mule}=(mul.east) in (mul.east)--(3.5,\y{mule})
  \end{scope}
}
#+end_export

#+name: fig:dualSec
#+caption: \dots
\begin{figure*}
\centering
\begin{tikzpicture}
 \begin{scope}[xshift=-5cm]
    \node (primal) at (4, 2.155) {\(\mathbf{s}\)}
    \node (jacobian) at (4, .7) {\(\mathbf{J}_{\mathbf{s}}\)}
    \draw[thick, ->] (primal.east)--(5, 2.155)
    \draw[thick, ->] (jacobian.east)--(5, .7)
  \end{scope}
  
  \diffop{faust=A,math=g}
  \node (primal) at (4.8, 2.155) {\(\mathbf{z} = g(\mathbf{s})\)}
  \node (jacobian) at (4.8, .7) {\(\mathbf{J}_{\mathbf{z}} = \mathbf{J}_{\mathbf{s}}\mathbf{J}_{g}\)}
  \draw[thick] (3.5, 2.155)--(primal.west)
  \draw[thick] (3.5, .7)--(jacobian.west)
  \draw[thick, ->] (primal.east)--(6.2, 2.155)
  \draw[thick, ->] (jacobian.east)--(6.2, .7)
  
  \begin{scope}[xshift=6.2cm]
    \diffop{faust=B,math=f}
    \node (primal) at (5, 2.155) {\(\mathbf{y} = f(g(\mathbf{s}))\)}
    \node (jacobian) at (5,.7) {\(\mathbf{J}_{\mathbf{y}} = \mathbf{J}_{\mathbf{s}}\mathbf{J}_{g}\mathbf{J}_{f}\)}
    \draw[thick] (3.5, 2.155)--(primal.west)
    \draw[thick] (3.5, .7)--(jacobian.west)
  \end{scope}
\end{tikzpicture}
\end{figure*}

Implicit in cref:eq:semanticTransformation,eq:diffSemantic are the
input and output signals of the Faust block diagram, \(D\); indeed, we
can write
\begin{align}
D &= \big\|_{l=1}^{i_{D}}\,\_ : D\label{eq:diffSeqIn} \\
D &= D : \big\|_{l=1}^{o_{D}}\,\_ \;,
\end{align}
where \(i_{D}\) and \(o_{D}\) are, respectively, the number of input and
output signals of \(D\). Applying derivative transformation to
cref:eq:diffSeqIn, and bringing cref:eq:diffpar,eq:diffidentity2 to
bear,
#+NAME: eq:diffImplicitIn
\begin{align}
\Delta_{N}\llbracket D \rrbracket &= \Delta_{N}\llbracket \big\|_{l=1}^{i_{D}}\,\_ : D \rrbracket \nonumber \\
  &= \big\|_{l=1}^{i_{D}}\big\|_{m=1}^{N+1}\,\_ : \Delta_{N}\llbracket D \rrbracket \nonumber \\
  &= \big\|_{l=1}^{i_{D}}\Delta_{N}\llbracket \_ \rrbracket : \Delta_{N}\llbracket D \rrbracket\;.
\end{align}
The arithmetical transparency of sequential composition holds in the
general case; expading on the semantic scheme in cref:eq:diffSemantic,
\(\Delta_{N}\llbracket A : B \rrbracket\) is analogous to finding the
derivative of the composition of /two/ functions, \(g\) and \(f\):
#+NAME: eq:diffSeqSemantic
\begin{equation}
\begin{split}
\Delta_{N}\llbracket A : B \rrbracket(\langle{}\mathbf{s}, &\;\mathbf{J}_{\mathbf{s}}\rangle)
  \to (\langle{}\mathbf{y}, \;\mathbf{J}_{\mathbf{y}}\rangle) \\[5pt]
\langle{}\mathbf{y}(t), \;\mathbf{J}_{\mathbf{y}}(t)\rangle
  &= f(g(\langle\mathbf{s}(t), \;\mathbf{J}_{\mathbf{s}}(t)\rangle)) \\
  &= \big\langle{}f(g(\mathbf{s}(t))), \;\mathbf{J}_{\mathbf{s}}(t)\mathbf{J}_{g}(t)\mathbf{J}_{f}(t)\big\rangle\;.
\end{split}
\end{equation}
As illustrated in cref:fig:dualSec, the composition of functions of dual
signals has the effect of primal function composition on the one hand,
and application of the chain rule of differentiation on the other, and
works so long as \(o_{A} = i_{B}\),[fn:2] thus
#+NAME: eq:diffSeq
\begin{equation}
\Delta_{N}\llbracket A : B \rrbracket = \Delta_{N}\llbracket A \rrbracket : \Delta_{N}\llbracket B \rrbracket\;.
\end{equation}

*** Split Composition

Composing \(A\) and \(B\), \(i_{B} = ko_{A}\), with the split
composition operator (see cref:fig:split) duplicates the output
signals of \(A\), \(k\) times, passing each group of duplicates to
\(B\), in order:
#+NAME: eq:split
\begin{equation}
A <: B = A : \big\|_{l=1}^{o_{A}}\,\_ <: \big\|_{m=1}^{k}\big\|_{l=1}^{o_{A}}\,\_ : B\;.
\end{equation}
Signal order is maintained within each group of duplicates, so,
applying derivative transformation to both sides of cref:eq:split, and
recalling cref:eq:diffImplicitIn,
#+NAME: eq:diffsplit
\begin{align}
&\quad\;\Delta_{N}\llbracket A <: B \rrbracket \nonumber\\
  &= \Delta_{N}\llbracket A \rrbracket :
    \big\|_{l=1}^{o_{A}}\Delta_{N}\llbracket \_ \rrbracket <:
    \big\|_{m=1}^{k}\big\|_{l=1}^{o_{A}}\Delta_{N}\llbracket \_ \rrbracket :
    \Delta_{N}\llbracket B \rrbracket \nonumber \\
  &= \Delta_{N}\llbracket A \rrbracket <: \Delta_{N}\llbracket B \rrbracket \;.
\end{align}

*** Cut Primitive
:PROPERTIES:
:CUSTOM_ID: sec:cut
:END:

The cut primitive, notated !, terminates a signal, and combinations of
the cut and identity functions can be used to create complex signal
routings.  For instance, to interleave two groups of two signals, such
that \(s_{1,1},s_{1,2},s_{2,1},s_{2,2}\) becomes \(s_{1,1},s_{2,1},s_{1,2},s_{2,2}\), one may
write
#+NAME: eq:interleave22
\begin{equation}
\_,\_,\_,\_ \;<:\; \_,!,\_,!,!,\_,!,\_\;.
\end{equation}
To differentiate an expression of this kind, the appropriate
accompaniment to cref:eq:diffidentity2,eq:diffsplit for the derivative
of the cut primitive is
#+NAME: eq:cut
\begin{equation}
  \begin{split}
    \Delta_{N}\llbracket ! \rrbracket &= \big\|_{l=1}^{N+1}!\;.
  \end{split}
\end{equation}
A general interleaving function, as used in cref:lst:diffAdd, can be
written using the cut and identity primitives, plus parallel and split
composition (see cref:lst:interleave). Comprising only differentiable
elements, it too is differentiable.
#+name: lst:interleave
#+caption: A Faust function that interleaves \(c\) groups of \(r\)
#+caption: signals into \(r\) groups of \(c\) signals. See cref:eq:interleave.
#+attr_latex: :options float=ht
#+begin_src faust :noeval
interleave(r, c) = par(l, r * c, _)
  <: par(m, r,
       par(n, c, cutpq(m, r - m - 1)))
with {
  cutpq(0, q) = _,par(l, q, !);
  cutpq(p, 0) = par(l, p, !),_;
  cutpq(p, q) =
    par(l, p, !),cutpq(0, q);
};
#+end_src

*** Delay Primitive
:PROPERTIES:
:CUSTOM_ID: sec:delay
:END:

Faust provides two primitives for delaying signals; here we will focus
on the ~mem~ primitive, which, when composed in sequence with a
signal, applies a one-sample delay.  In general, a fixed delay can be
represented as the convolution of a signal, \(s(x,t)\), with a
time-shifted impulse,
#+NAME: eq:delayAsConv
\begin{equation}
s(x, t-\tau) = s(x, t) \ast h(t)\;,\quad h(t) = \delta(t - \tau)\;.
\end{equation}
Taking the temporal-parametric Fourier transform of the derivative of
the convolution with respect to \(x\), then using the convolution and
differentiation properties of the transform
[cite:@mcclellanSignal2003], yields
#+NAME: eq:timeinvariantdiff
\begin{align}
&\quad\;\mathcal{F}\left[\frac{\partial{}}{\partial{}x}\left(s(x,t) \ast h(t)\right)\right] \nonumber\\
  &= j\omega_{x}S(j\omega_{x},j\omega_{t})H(j\omega_{t}) \\
  &= \mathcal{F}\left[\frac{\partial{}}{\partial{}x}s(x,t)\right]\cdot\mathcal{F}\left[h(t)\right]\;,
\end{align}
which demontsrates that differentiation is time-invariant, and allows
us to deduce the convolution rule of differentiation --- to
differentiate a convolution we need only differentiate one of the
functions being convolved. Applied to dual numbers,
#+NAME: eq:
\begin{equation}
\langle{}s,\nabla{}s\rangle\ast\langle{}h,\nabla{}h\rangle = \langle{}s\ast{}h, \nabla{}s\ast{}h\rangle\;,
\end{equation}
giving us the following semantic scheme:
#+NAME: eq:diffmem
\begin{equation}
  \begin{split}
    \Delta_{N}\llbracket\texttt{mem}\rrbracket &\;:\; \mathbb{S}_{d} \to \mathbb{S}_{d} \\
    \Delta_{N}\llbracket\texttt{mem}\rrbracket(\langle{}s, \nabla{}s\rangle) &= (\langle{}y, \nabla{}y\rangle) \\
    \langle{}y(t),\nabla{}y(t)\rangle &= \langle{}s(t-1),\nabla{}s(t-1)\rangle\;.
  \end{split}
\end{equation}

*** Recursive Composition

The differentiability of recursive filters is well-established
[cite:@shynkAdaptive1989] --- the derivative of a recursion is itself
recursive. To establish a differentiation rule for Faust's recursive
composition operator (see cref:fig:rec), we can take a similar
approach to cref:eq:diffSeqSemantic, making \(g\) an inner function
that acts upon a vector of past output signals:
#+NAME: eq:dualrec
\begin{equation}
\begin{split}
&\langle{}\mathbf{y}(t),\,\mathbf{J}_{\mathbf{y}}(t)\rangle = \bigg\langle{}f(\mathbf{s}(t),g(\mathbf{y}(t-1))), \\
  &\mathbf{J}_{\mathbf{s}}(t)\frac{\partial{}f}{\partial{}\mathbf{s}(t)} +
  \mathbf{J}_{\mathbf{y}}(t-1)\mathbf{J}_{g}(t-1)\frac{\partial{}f}{\partial{}g(\mathbf{y}(t-1))}\bigg\rangle\;.
\end{split}
\end{equation}
In the absence of a feedforward component (notwithstanding the fact
that such a system would produce constant output signals of value zero
for all \(t\)), the result simplifies to
#+NAME: eq:all-pole
\begin{equation}
\langle{}f(g(\mathbf{y}(t-1))),\; \mathbf{J}_{\mathbf{y}}(t-1)\mathbf{J}_{g}(t-1)\mathbf{J}_{f}(t-1)) \rangle\;,
\end{equation}
which, comparing with cref:eq:diffSeqSemantic, tells us that recursive
composition is simply /sequential composition through time/, and
points to the implicit, and trivially-differentiable, one-sample delay
introduced by Faust's recursive composition operator. We can conclude
that
#+NAME: eq:diffrec
\begin{equation}
\Delta_{N}\llbracket A \sim B \rrbracket = \Delta_{N}\llbracket A \rrbracket \sim \Delta_{N}\llbracket B \rrbracket\;.
\end{equation}

*** Merge Composition

Merge composition (cref:fig:merge) entails summation
[cite:@orlareySyntactical2004], thus the addition rule of
differentiation must be obeyed when merging dual signals. A merge \(A
:> B\), can be viewed as an interleaving of the outputs of \(A\) into
\(i_{B}\) groups of \(k=o_{A}/i_{B}\) signals, followed by a merging of each
group into one signal path:
#+NAME: eq:expandedMerge
\begin{equation}
A :> B = A : \chi(i_{B},k) : \big\|_{l=1}^{i_{B}}\left(
  \big\|_{m=1}^{k}\,\_ :> \_
\right) : B\;.
\end{equation}
Clearly \(\Delta_{N}\llbracket A:>B \rrbracket\) cannot be defined
recursively, however. Note that for \(i_{B}=1\) the right hand side of
cref:eq:expandedMerge reduces to \(A:>\_\), essentially the term in
parenthesis, which can be handled thanks to the definitions in
cref:eq:diffidentity2,eq:diffpar,
#+NAME: eq:mergeOntoOne
\begin{equation}
\Delta_{N}\llbracket \big\|_{m=1}^{k}\,\_ :> \_ \rrbracket
  = \big\|_{m=1}^{k}\Delta_{N}\llbracket \_ \rrbracket :> \Delta_{N}\llbracket \_ \rrbracket\;.
\end{equation}
The order of signals is maintained, so the resulting dual signal is
guaranteed to be of the desired form
\(\left\langle{}\sum_{m=1}^{k}s_{m},\sum_{l=m}^{k}\nabla{}s_{m}\right\rangle\).  For the full
expression for differentiable merge composition, see
cref:eq:diffmerge.

** Summary

#+name: lst:neuron
#+caption: \dots
#+attr_latex: :float t
#+begin_src faust :noeval
// diff.lib
neuron(NINPUTS, activationFunction) =
  routes.crossNM(NINPUTS + 1, NINPUTS)
  : Y
with {
  WX = routes.interleave(NINPUTS, 2)
    : sum(i, NINPUTS, *);
  b = _;
  Y = WX + b : activationFunction;
};

activations = environment {
  sigmoid = *(-1) : 1 / (1 + exp);
  //...
}

// neuron.dsp
NW = 3; // Number of weights.

process = par(i, NW + 1,
      wb.var(i + 1)),par(i, NW, _)
  : d.diffInM(
    d.neuron(N, d.activations.sigmoid),
  N)
with {
    wb = weightsAndBiases((1, NW));
    d = df.env(wb);
};
#+end_src

The rules for differentiating Faust's composition operators are
collected in cref:fig:diffRules. With the implementation of these
rules via pattern matching, Faust programs can be recursively
decomposed according to their composition operators, and re-composed
in differentiable fashion. For example:
#+NAME: eq:diffComposition
\begin{equation}
\begin{split}
\Delta_{N}\llbracket (A,C):B \rrbracket
  &= \Delta_{N}\llbracket A,C \rrbracket : \Delta_{N}\llbracket B \rrbracket \\
  &= \Delta_{N}\llbracket A \rrbracket , \Delta_{N}\llbracket C \rrbracket : \Delta_{N}\llbracket B \rrbracket\;.
\end{split}
\end{equation}
For example, an artificial neuron of the form \(y =
\sigma(\mathbf{WX}+b)\), such as could be used in a binary classification
problem, can be created as per cref:lst:neuron; derivative
transformation occurs within the wrapper function
~d.diffInM~. Moreover, algorithms of this sort, consisting of
differentiable primitives, plus our now-differentiable composition
operators, can be automatically differentiated to arbitrary order,
\(\Delta_{N}\llbracket{\scriptstyle\Delta_{N}\llbracket
\cdots}{\scriptscriptstyle\Delta_{N}\llbracket \cdot \rrbracket}{\scriptstyle \cdots
\rrbracket}\rrbracket\).

*** Limitations

Although the described scheme renders a great deal of existing Faust
code differentiable, certain primitives and language constructs
cannot, at present, be subjected to AD.

**** Route primitive
:PROPERTIES:
:UNNUMBERED:
:END:
Faust's ~route~ primitive cannot be pattern-matched, which precludes
the derivative transformation of arbitrary signal routing expressions
based on that primitive, such as those in the =routes.lib=
library.[fn:4] As described in cref:sec:cut, however, such expressions
can be recreated via judicious use of the identity and cut primitives,
and common routing tasks like interleaving can be achieved
differentiably with code such as in cref:lst:interleave.

**** User interface primitives
:PROPERTIES:
:UNNUMBERED:
:END:

The obvious derivative transformation of a user interface element is
to an input variable, i.e. a parameter for optimisation such as a
weight in a neural network. Input variables must be identifiable so as
create the appropriate corresponding dual signal
\(\big\langle{}x_{i},\begin{bmatrix}0 & \cdots & \frac{\partial{}x_{i}}{\partial{}x_{i}} & \cdots & 0\end{bmatrix}^{T}\big\rangle\).

Though matchable in principle, to perform a derivative transformation
on a user interface element it is necessary to pattern-match on all of
its parameters. For instance, to transform the slider ~hslider("x1", 0,
-1, 1, .01)~, whose parameters are a string identifier, inital value,
minimum, maximum and step, it is necessary to write ~diff(hslider("x1",
0, -1, 1, .01))~. Clearly, in the general case, this is not practical.

The approach proposed is to move input variables out of the general AD
transformation; a wrapper function is used to indicate the number of
input signals (whose gradients are zero vectors) and input variables
received by the main body of the differentiated block diagram (see
cref:lst:neuron,lst:iir). Though imposing a certiain coding convention, we
consider this comparable with the particular requirements imposed by
PyTorch, TensorFlow, and JAX, with their respective dedicated
datatypes for learnable parameters.

**** Lambda abstractions
:PROPERTIES:
:UNNUMBERED:
:END:

#+name: lst:iir
#+caption: \dots
#+attr_latex: :float t
#+begin_src faust :noeval
N = 3; // filter order.

// ...Set up AD vars & environment...

// Differentiable TDF IIR filter.
iir = ((sub,par(l, N - 1, _))
  ~ (fir(N - 1))),par(l, N, _)
  : _,par(l, N - 1, !),par(l, N, _)
  : fir(N)
with { 
  sub = _,_ <: !,_,_,! : -;
  fir(n) =
    (_ <: par(l, n, _)),par(l, n, _) 
    : routes.interleave(n, 2) 
    : par(i, n,
          seq(m, i, mem),_ : *) 
    :> _;
};

// Send a signal input, and coef-
// -ficients, to the diffed filter.
process = _,coeffs : d.diffInN(iir, 1)
with {
    coeffs = par(i, 2 * N - 1,
                 vars.var(i + 1));
};
#+end_src

Perhaps the most problematic limitation posed is that of Faust's
lambda abstractions, which can be neither pattern-matched, nor
differentiated. The ~iir~ function in =filters.lib=, for example, is
implemented in transposed-direct-form [cite:@smithIntroduction2008]
and defined with the unapplied lambda abstraction ~ma.sub~, which
accepts two arguments, ~x~ and ~y~, and returns the result
~y-x~. Attempting \(\Delta_{N}\llbracket \texttt{sub} \rrbracket\), the
function is essentially treated as a numerical constant, and given a
vector of zeros as its gradient. Fortunately, constructions of this
kind --- in this case reversing signal order before applying some
primitive operation --- are effectively matters of signal-routing and,
as described in cref:sec:cut, can be recreated using differentiable
primitives. See cref:lst:iir for a differentiable
transposed-direct-form IIR filter implementation.

* Examples                                                         :noexport:

Here are a few examples on LaTeX.

** Tables & figures

Tables can be created in various ways and for various purposes. The
example in cref:tb:firsttb shows basic trigonometric values
using booktabs formatting.

#+name: tb:firsttb
#+caption: This table shows a few trigonometric values.
#+attr_latex: :booktabs t :center t :align ccc :placement [t]
|---------+----------------+----------------|
| \(\theta\)   | \(\sin(\theta)\)    | \(\cos(\theta)\)    |
|---------+----------------+----------------|
| \(\pi/4\) | \(\sqrt{2}/2\) | \(\sqrt{2}/2\) |
| \(\pi/3\) | \(\sqrt{3}/2\) | \(1/2\)        |
| \(\pi/2\) | \(1\)          | \(0\)          |
|---------+----------------+----------------|

There are also various ways to include and even draw figures in
LaTeX. The most common is to include them. As is shown with
cref:fg:sincplot and cref:fg:AESspec_jet. LaTeX places the figures in
the optimal place in the document by its typesetting rules but
appropriate location can be hinted with [t], [b], and [h] arguments
(corresponding to top, bottom, and here). Combination of these are
also allowed. AES instructions recommend only placing figures and
tables at the top or the bottom of the document but getting the
preferred result might require some tuning by hand.

#+name: fg:sincplot
#+caption: This is an example of using the pdf format showing means
#+caption: and confidence intervals of audiometric data.
#+attr_latex: :placement [t] :width 0.95\columnwidth
[[./results.pdf]]

#+attr_org: :width 500
#+name: fg:AESspec_jet
#+caption: This is a spectrogram of a spectral delay filtered
#+caption: sawtooth waveform using the png format.
#+attr_latex: :placement [t] :width 0.95\columnwidth
[[./spectrogram.png]]

* Conclusion

We did it.

#+print_bibliography:

* Footnotes

[fn:4] \href{https://github.com/grame-cncm/faustlibraries/blob/65594336a3476d9a82a4df71e88c92f3e4b94821/routes.lib}{https://github.com/grame-cncm/faustlibraries/blob/master/routes.lib}

[fn:3] Note that in Faust, \(a\), the iterator index, starts from
zero. (What works in code does not necessarily transfer well to the
page.)

[fn:2] This condition did not apply in Faust's early days
[cite:@orlareySyntactical2004], but does at the time of writing.

[fn:1] Orlarey [cite:@orlareySyntactical2004] includes the identity
(_) and cut (!) blocks separately in this syntactic rule; here we will
consider these as belonging to the set of primitive blocks.

* Local Variables                                                  :noexport:

# Local Variables:
# org-latex-classes: (("aes-aimla-2025"
# "\\documentclass[conference,peer-reviewed,blind-review]{aesconf}
# [NO-DEFAULT-PACKAGES]"
# ("\\section{%s}" . "\\section*{%s}")
# ("\\subsection{%s}" . "\\subsection*{%s}")
# ("\\subsubsection{%s}" . "\\subsubsection*{%s}")
# ("\\paragraph{%s}" . "\\paragraph*{%s}")
# ("\\subparagraph{%s}" . "\\subparagraph*{%s}")))
# org-latex-src-block-backend: listings
# org-latex-pdf-process: ("latexmk -f -pdf -%latex \
#   -interaction=nonstopmode -shell-escape -output-directory=%o %f")
# End:
